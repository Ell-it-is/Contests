// time-limit: 3000
// problem-url: https://codeforces.com/contest/1894/problem/C
// ================= Header ================= //
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#ifdef DEBUG
#include "../template/debug.h"
#else
#define db(x...)
#endif

// ================= Solution =============== //


int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  int tt = 1;
  cin >> tt;
  while (tt--) {
    int n, k;
    cin >> n >> k;
    vector<int> b(n);
    for (int i = 0; i < n; i++) {
      cin >> b[i];
    }
    
  }
}
/* ================= Notes ================== //
   array 'a' must be a permutation of array 'b'
   Can I perform k left shifts to get array 'b' from some original 'a'?
   
*/


















