// time-limit: 1000
// problem-url: https://codeforces.com/problemset/problem/1864/C
// ================= Header ================= //
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#ifdef DEBUG
#include "../template/debug.h"
#else
#define db(x...)
#endif

// ================= Solution =============== //


int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  int tt = 1;
  cin >> tt;
  while (tt--) {
    int x;
    cin >> x;
    
  }
}
/* ================= Notes ================== //
   x -> 1
   Select any divisor of x (at most twice) and subtract that divisor from x

   How to find largest multiple of 2 <= x?
   Easy, you can just make a loop, but it can probably be done in constant time
   
*/


















