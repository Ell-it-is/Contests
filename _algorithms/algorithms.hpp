// Some possible improvments for my template:
// nahradit L, R -> tridou Range
// pokusit se nahradit substr, coz je v cyklu (n * m) na (n + m), automat regex
// rotate linear, fake vector pomoci iteratoru






























