// time-limit: 2000
// problem-url: https://atcoder.jp/contests/abc213/tasks/abc213_c
// ================= Header ================= //
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#ifdef DEBUG
#include "../template/debug.h"
#else
#define db(x...)
#endif

// ================= Solution =============== //

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  int tt = 1;
  //cin >> tt;
  while (tt--) {
    
  }
}
/* ================= Notes ================== //
   if row number is not mentioned in given points -> row does not exists
   if row does not exists -> all points that have higher row number should be placed row above
   # of rows you place the given point above is the number of rows that are missing above
*/


















